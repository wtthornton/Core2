#pragma once
// Copy to secrets.h (gitignored for Wi-Fi). Paste AF block from host mint script.
// AF_API_KEY = afp_ project desk key (not admin / not af_*). Never put admin on device.
#define CORE2_WIFI_SSID "your-ssid"
#define CORE2_WIFI_PASS "your-pass"
#define AF_BASE_URL "http://192.168.1.207:8010"
#define AF_PROJECT_SLUG "core2"
#define AF_API_KEY "afp_paste_from_af_host_mint"
// After first Save on Beam setup (or `cfg save` over USB), NVS overrides these.
